declare type Parameter = {
  logicalId: string;
  name: string;
  description: string;
  dev: {
    value: string;
  };
  prod: {
    string;
  };
};

const createParameter = (param: Parameter) => {
  const parameter = ``;
};
